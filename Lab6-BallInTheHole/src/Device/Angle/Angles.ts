import Angle from "./Angle.js";

class Alpha extends Angle {
    constructor(value: number) {
        super(value);
    }
}

class Beta extends Angle {
    constructor(value: number) {
        super(value);
    }
}

class Gamma extends Angle {
    constructor(value: number) {
        super(value);
    }
}

export {Alpha, Beta, Gamma};